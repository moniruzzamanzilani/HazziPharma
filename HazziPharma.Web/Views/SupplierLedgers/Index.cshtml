@model HazziPharma.Web.ViewModels.SupplierLedgerViewModel

@{
    ViewData["Title"] = "Supplier Ledger";
}

<h3 class="mb-3">Supplier Ledger</h3>

<form method="get">

    <div class="row">

        <div class="col-md-4">

            <label class="form-label">Supplier</label>

            <select asp-for="SupplierId"
                    asp-items="Model.Suppliers"
                    class="form-select">

                <option value="">-- Select Supplier --</option>

            </select>

        </div>

        <div class="col-md-2 d-flex align-items-end">

            <button class="btn btn-primary w-100">
                Show Ledger
            </button>

        </div>

    </div>

</form>

<hr />

<table class="table table-bordered">

    <thead>

        <tr>

            <th>Date</th>
            <th>Type</th>
            <th>Voucher</th>
            <th>Debit</th>
            <th>Credit</th>
            <th>Balance</th>

        </tr>

    </thead>

    <tbody>

        @foreach (var item in Model.Ledger)
        {
            <tr>

                <td>@item.Date.ToString("dd-MMM-yyyy")</td>

                <td>@item.Type</td>

                <td>@item.VoucherNo</td>

                <td>@item.Debit.ToString("0.00")</td>

                <td>@item.Credit.ToString("0.00")</td>

                <td>@item.Balance.ToString("0.00")</td>

            </tr>
        }

    </tbody>

</table>